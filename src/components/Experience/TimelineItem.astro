---
interface Props {
  company: string;

  title: string;

  duration: string;

  highlights: string[];

  stack: string[];
  printable?: boolean;
}

const {
  company,

  title,

  duration,

  highlights,

  stack,
  printable = false,
} = Astro.props;
const visibleHighlights = printable ? highlights.slice(0, 4) : highlights;
---

<div class="timeline-item">
  <div class="timeline-dot"></div>

  <div class="timeline-content">
    <div class="timeline-header">
      <div>
        <h3>
          {company}
        </h3>

        <p>
          {title}
        </p>
      </div>

      <span>
        {duration}
      </span>
    </div>

    <ul>
      {visibleHighlights.map((item) => <li>{item}</li>)}
    </ul>

    <div class="tech-stack">
      {stack.map((item) => <span>{item}</span>)}
    </div>
  </div>
</div>
