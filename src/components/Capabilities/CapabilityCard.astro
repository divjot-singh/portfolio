---
interface Props {
  title: string;
  items: string[];
}

const { title, items } = Astro.props;
---

<article class="capability-card">
  <h3>{title}</h3>

  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</article>
